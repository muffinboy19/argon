<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Argon" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.Argon" parent="Base.Theme.Argon" >
    </style>

    <style name="bottomNavBar" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="android:background">@android:color/white</item>
        <item name="android:icon">@color/black</item>
        <item name="android:textColor">@color/black</item>
        <item name="itemHorizontalTranslationEnabled">false</item>
    </style>
</resources>